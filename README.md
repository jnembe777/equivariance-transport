# How far can symmetry help? — Companion repository

Code and numerical experiments accompanying the paper:

> **How far can symmetry help? Phase transitions and symmetry selection in
> sparse functional data analysis.**
> Jocelyn Nembé, Modeling and Calculus Laboratory, Libreville, Gabon.
> arXiv: [TODO — insérer l'identifiant arXiv de la v2.15]

The paper studies the sharp phase transition of the covariance surface in
sparse functional data analysis at the critical sampling intensity
m*_n ≍ n^{1/(2β)}, quantifies what a symmetry (equivariance) assumption buys,
and closes the minimax gap at the stationary endpoint via positive spectral
thresholding, with rate (nm²)^{-(2β+1)/(2β+2)} + 1/n.

## Contents

```
scripts/    numerical benchmarks and real-data analysis (Python)
figures/    figures generated by the scripts
results/    numerical outputs (JSON)
data/       data provenance (no raw data stored; see data/README.md)
```

## Scripts ↔ results in the paper

| Script | What it does | Output | Paper |
|---|---|---|---|
| `Gap_bench_sanity.py` | Sanity checks: dyadic-block mass lemma (β = 1) and bias/variance scales of the spectral estimator λ̂ₖ (n = 400, m = 8, 40 reps) | console tables | dyadic mass lemma and spectral-estimator moment bounds [TODO: n° de lemme/prop.] |
| `Gap_bench_race1.py` | Rate race, single member: thresholded spectral estimator vs oracle-bandwidth smoother; empirical slopes vs targets (2β+1)/(2β+2) and 2β/(2β+1) | console | "Closing the gap at the stationary endpoint" [TODO: n° de section] |
| `Gap_bench_race2.py` | Rate race over a two-member family (sup-risk); separation of the two floors n^{1/(4β)} vs n^{1/(4β+2)} | console | same section, sup-risk statement [TODO] |
| `Art1_analyse_stationnaire.py` | El Niño data: hold-out risk of the q-fold symmetrized estimator targeting the stationary component Π₁₂C, 60 splits; saturation threshold q_sat vs bandwidth h | console table | real-data section (stationary target) [TODO: n° de table] |
| `Art1_analyse_elnino.py` | El Niño data: full analysis (refusal on the full covariance, gains on the stationary component), produces the figure and the numbers cited in the text | `figures/fig_elnino.pdf`, `results/elnino_numbers.json` | El Niño figure and quoted percentages (+12 % refusal / 46 % gain) [TODO: n° de figure] |

All random experiments use fixed seeds (`numpy.random.default_rng(0)` or
`(1)`), so every number and the figure are reproducible bit-for-bit.

## Reproducing

Python ≥ 3.10.

```bash
pip install -r requirements.txt
./run_all.sh          # runs everything, ~ a few minutes
```

or run any script individually from `scripts/`:

```bash
cd scripts
python Art1_analyse_elnino.py   # writes fig_elnino.pdf + elnino_numbers.json
```

`run_all.sh` moves the generated figure to `figures/` and the JSON to
`results/`.

## Data

The real-data section uses the classical **El Niño sea-surface temperature
dataset** (monthly SST, 1950–2010) as shipped with `statsmodels`
(`statsmodels.datasets.elnino`). No download and no local data files are
required; see `data/README.md` for provenance and licensing.

## Citing

See `CITATION.cff`, or cite the arXiv preprint directly.

## License

MIT for all code in this repository (see `LICENSE`). The manuscript itself
is distributed via arXiv and is not part of this repository.
